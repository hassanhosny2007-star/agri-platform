name: فحص تنبيهات الطقس كل ساعة
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: استدعاء Edge Function
        run: |
          curl -X POST \
            "https://tzpoytzacshvmpzfclfv.supabase.co/functions/v1/check-weather-alerts" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
